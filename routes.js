const express= require('express');
const route = express.Router(); 

// rotas da homepage

route,get('/', (req, res)=>{
    res.send(`<form> method=`)
})
route.post('/',()=>{
    res.send('<h1> nova rota de post (home) </h1>')
})
fafafafa
modeul.exports = route;